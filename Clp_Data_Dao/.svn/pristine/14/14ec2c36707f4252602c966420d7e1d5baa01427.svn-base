package com.bestinfo.dao.encoding;

import com.bestinfo.bean.encoding.KDrawProcessStatus;
import java.util.List;
import org.springframework.jdbc.core.JdbcTemplate;

/**
 *
 * @author hhhh
 */
public interface IKDrawProStatus {
    /**
     * 查询快开游戏期进度状态数据列表
     * @param jdbcTemplate
     * @return 列表数据
     */
    public List<KDrawProcessStatus> selectKDrawProStatus(JdbcTemplate jdbcTemplate);
    
    /**
     * 修改快开游戏期进度状态数据
     * @param jdbcTemplate
     * @param kp
     * @return 
     */
    public int updateKDrawProStatus(JdbcTemplate jdbcTemplate, KDrawProcessStatus kp) ;

    /**
     * 根据进度编号获取快开游戏期进度状态
     *
     * @param jdbcTemplate
     * @param kenoProcessStatus 进度编号
     * @return 快开游戏期进度状态
     */
//    public KDrawProcessStatus getKDrawProStatusById(JdbcTemplate jdbcTemplate, int kenoProcessStatus);

    /**
     * 根据进度编号修改快开游戏期进度状态
     *
     * @param jdbcTemplate
     * @param kenoProcessStatus 进度编号
     * @param kdrawStatusName 进度状态名称
     * @param workStatus 工作状态
     * @return 成功-影响记录数 失败-(-1)
     */
//    public int updateKDrawProStatus(JdbcTemplate jdbcTemplate, int kenoProcessStatus, String kdrawStatusName, int workStatus);

    /**
     * 新增快开游戏期进度状态
     *
     * @param jdbcTemplate
     * @param kenoProcessStatus 进度编号
     * @param kdrawStatusName 进度状态名称
     * @param workStatus 工作状态
     * @return 成功-影响记录数 失败-(-1)
     */
//    public int insertKDrawProStatus(JdbcTemplate jdbcTemplate, int kenoProcessStatus, String kdrawStatusName, int workStatus);

    /**
     * 根据进度编号删除快开游戏期进度状态
     *
     * @param jdbcTemplate
     * @param kenoProcessStatus 进度编号
     * @return 成功-影响记录数 失败-(-1)
     */
//    public int deleteKDrawProStatusById(JdbcTemplate jdbcTemplate, int kenoProcessStatus);
}
