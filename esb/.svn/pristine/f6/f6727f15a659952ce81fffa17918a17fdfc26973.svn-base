package cn.edugate.esb.redis.dao;

import java.util.List;

import cn.edugate.esb.entity.Picture;

public interface RedisPictureDao {

	boolean add(Picture user);

	boolean add(List<Picture> users);

	boolean delete(Picture user);

	boolean deleteAll();

	Picture getPictureById(Integer id, Object object);
}
